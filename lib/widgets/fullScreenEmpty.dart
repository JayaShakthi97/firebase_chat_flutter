import 'package:flutter/material.dart';

class FullScreenEmptyWidget extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Container(
      child: Center(
        child: Text('No Data'),
      ),
    );
  }
}
